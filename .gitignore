# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# Phase 4: build-time HTTP cache for scripts/fetch_standard_titles.py, so a
# re-run does not re-fetch bis.gov.in or archive.org. Derived, never evidence.
backend/.cache/

# Environment / secrets
.env
.env.local

# Node / frontend
node_modules/
dist/
frontend/.vite/
*.tsbuildinfo

# Editors / OS
.vscode/
.idea/
.DS_Store

# Phase 9: a normal eval_retrieval.py run writes here; the committed baseline moves only with --write-baseline.
backend/tests/data/eval_latest.json
